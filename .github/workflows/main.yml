name: Run Tests


on:
  push:
    branches:
main

pull_request:
  branches:
main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository
      
name: Checkout code
      uses: actions/checkout@v3
    # Set up .NET Core (if your project uses .NET, for example)
name: Setup .NET Core
    uses: actions/setup-dotnet@v3
    with:
      dotnet-version: '8.x' # Adjust this version as needed

Run tests
name: Run tests
run: cd xunittests && dotnet test
